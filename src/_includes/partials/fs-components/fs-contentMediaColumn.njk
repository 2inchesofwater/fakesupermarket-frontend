{% from "./fs-contentMediaItem.njk" import mediaItem %}

{% macro contentMediaColumn(data) %}

  <div class="fs-contentMedia fs-contentMediaColumn">
    {% for item in data %}
      <figure class="fs-columnMedia__item">
        {{ mediaItem(item) }}

        {% if item.caption %}
          <figcaption class="fs-columnMedia__caption">{{ item.caption | safe }}</figcaption>
        {% endif %}
      </figure>
    {% endfor %}
  </div>

{% endmacro %}